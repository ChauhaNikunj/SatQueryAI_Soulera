# ══════════════════════════════════════════════════════════════
#  SatQuery AI Backend — Python Dependencies
#  ISRO Problem Statement 26167 | SIH 2026
# ══════════════════════════════════════════════════════════════

# ── Web framework ─────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Core ML / numerical ───────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.26.0
Pillow>=10.3.0

# ── RemoteCLIP / OpenCLIP (Task 5 encoder) ───────────────────
open_clip_torch>=2.24.0

# ── Geospatial I/O (GeoTIFF loading) ─────────────────────────
rasterio>=1.3.0
# gdal>=3.8.0           # uncomment if rasterio is unavailable

# ── HuggingFace ecosystem (for future Qwen2.5-VL wiring) ─────
transformers>=4.40.0
accelerate>=0.29.0
bitsandbytes>=0.43.0       # 4-bit quantisation for Qwen
qwen-vl-utils>=0.0.8       # Qwen2.5-VL processor helper

# ── PEFT / LoRA (optional fine-tuning) ────────────────────────
peft>=0.10.0
trl>=0.8.0

# ── Timm (backbone utilities) ────────────────────────────────
timm>=1.0.0

# ── HTTP client (Ollama proxy, optional) ─────────────────────
httpx>=0.27.0
